language: general
sudo: required
services:
  - docker

before_install:
  - docker build -t chrislynch96/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true chrislynch96/docker-react npm run test